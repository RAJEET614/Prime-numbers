#include<stdio.h>
int is prime(int,int);
int main()
{   
  int num,prime;
  printf("Enter a positive number:");
  scanf("%d",&num);
  prime=is prime(num.num/2);
    if(prime==1)
      printf("%d prime number",num);
  { 
    else
      printf("%d not a prime number",num);
       return 0;
  }
  int is prime(int num,inti){
    if(i==1)
    {
      return 1;
    }
    
    
        
 
